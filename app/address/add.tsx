import { View, Text } from "react-native";
import React from "react";

type Props = {};

const Add = (props: Props) => {
  return (
    <View>
      <Text>Add</Text>
    </View>
  );
};

export default Add;
